# To-Do-List-Application.2
# 📝 To-Do List Application (Console-Based)

A simple Python console-based To-Do list application that allows you to **add**, **view**, and **remove** tasks.  
All tasks are stored in a text file, making the list persistent even after closing the program.

# 🚀 Features
- Add tasks
- View all tasks
- Remove tasks by task number
- Tasks saved in `tasks.txt` for persistence
- Simple and clean CLI interface

# 🛠 Tools Used
- Python 3+
- VS Code or any text editor
- Terminal / Command Prompt

📘 How It Works
- Tasks are stored in a list.
- Tasks are saved and loaded from **tasks.txt**.
- The app uses a loop to keep running until you choose Exit.

# 📌 Example Output
--- To-Do Menu ---

View Tasks

Add Task

Remove Task

Exit
Enter your choice (1-4): 2
Enter task to add: Complete Python DSA
Task added successfully.


# 💡 Future Improvements (Optional)
- Add task editing
- Add task completion status
- Add due dates
- Make a GUI version using Tkinter

# 🎯 Outcome
A fully functional **persistent CLI To-Do list application** using Python basics.
